{{ include ('template.twig') }}
{{ include ('backend/navAdminView.twig') }}

<h1 data-toggle="collapse" data-target="#memberList" aria-expanded="false" aria-controls="memberList" class="title-hover">LES MEMBRES</h1>

    <section class="collapse show" id="memberList">
        <table class="table table-hover ">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Nom</th>
                    <th>Prénom</th>
                    <th>Pseudo</th>
                    <th>statut</th>
                </tr>
            </thead>

            {% for user in users %}
                <tr onclick="document.location='/user/{{user.id}}'">
                    <a href="/profil/{{user.id}}">
                        <td>{{user.id}}</td>
                        <td>{{user.lname}}</td>
                        <td>{{user.fname}}</td>
                        <td>{{user.pseudo}}</td>
                        <td>{{user.status}}</td>
                    </a>
                </tr>
            {% endfor %}

        </table>
    </section>


